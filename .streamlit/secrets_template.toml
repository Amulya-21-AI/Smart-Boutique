# Copy this file to secrets.toml and fill in real values.
# secrets.toml is listed in .gitignore and will never be committed.
# On cloud platforms, enter these values in the platform's Secrets / Env vars UI.

ANTHROPIC_API_KEY = "sk-ant-api03-..."
