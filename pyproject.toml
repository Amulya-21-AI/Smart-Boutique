[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "smart-boutique"
version = "1.0.0"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["database", "utils", "genai", "ML"]
