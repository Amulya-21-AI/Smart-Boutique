[build]
builder = "nixpacks"

[deploy]
startCommand = "PYTHONPATH=/app python -m streamlit run Main.py --server.port $PORT --server.address 0.0.0.0"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
